import { IconProps } from '@chakra-ui/react';

type SeparatorStyleProps = Record<'icon', IconProps>;

export const separatorStyles: SeparatorStyleProps = {
    icon: {
        color: 'gray.800',
    },
};

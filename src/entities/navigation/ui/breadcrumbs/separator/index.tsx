import { ChevronRightIcon } from '@chakra-ui/icons';
import { FC } from 'react';

import { separatorStyles as styles } from './index.styles';

export const Separator: FC = () => <ChevronRightIcon {...styles.icon} />;

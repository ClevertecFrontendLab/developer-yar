import { BoxProps } from '@chakra-ui/react';

type MobileTabBarStyleProps = {
    mobileTabBarWrapper: BoxProps;
};

export const mobileTabBarStyles: MobileTabBarStyleProps = {
    mobileTabBarWrapper: {
        zIndex: 'docked',
    },
};

import { TextProps } from '@chakra-ui/react';

type AuthResetCredentialsModalStyleProps = {
    headerText: TextProps;
};

export const authResetCredentialsModalStyles: AuthResetCredentialsModalStyleProps = {
    headerText: {
        margin: 'auto',
        maxW: 60,
    },
};

import { BoxProps } from '@chakra-ui/react';

type ModalOverlayStyleProps = {
    overlay: BoxProps;
};

export const modalOverlayStyles: ModalOverlayStyleProps = {
    overlay: {
        layerStyle: 'overlay',
    },
};

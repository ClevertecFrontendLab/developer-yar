import { StackProps } from '@chakra-ui/react';

export type ModalContentGroupStyleProps = {
    contentGroup: StackProps;
};

export const modalContentGroupStyles: ModalContentGroupStyleProps = {
    contentGroup: {
        gap: 4,
        w: 'full',
    },
};

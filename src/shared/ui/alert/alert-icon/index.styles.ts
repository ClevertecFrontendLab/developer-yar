import { AlertIconProps } from '@chakra-ui/react';

export type AlertIconStyleProps = {
    alertIcon: AlertIconProps;
};

export const alertIconStyles: AlertIconStyleProps = {
    alertIcon: {
        boxSize: 6,
        color: 'white',
    },
};

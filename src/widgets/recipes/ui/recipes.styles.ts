import { GridProps } from '@chakra-ui/react';

type RecipesStyleProps = {
    container: GridProps;
};

export const recipesStyles: RecipesStyleProps = {
    container: { layerStyle: 'gridLayout' },
};
